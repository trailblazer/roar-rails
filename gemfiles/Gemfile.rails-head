source "http://rubygems.org"

# Specify your gem's dependencies in roar-rails.gemspec
gemspec path: '../'

#use the whole rails, because there is no way to get only railties from git
gem 'rails', :git => 'git://github.com/rails/rails.git'
